const create = () => {};
const read = () => {};
const update = () => {};
const remove = () => {};

const execute = (req, res) => {};

export const api = (req, res) => {
  const { appid, version, schemas, idcmd } = req.params;

  if (req.method === 'GET') {
  }
};
